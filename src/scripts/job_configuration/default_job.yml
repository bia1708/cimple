- job:
    name: with_props
    project-type: pipeline
    sandbox: true
    properties:
      - inject:
          keep-system-variables: true
          properties-file: "../../artifacts/properties_file.props"
    dsl:
      - script:
          !include-raw job.dsl
    # pipeline-scm:
    #   scm:
    #     - hg:
    #         url: "https://github.com/bia1708/cimple"
    #         clean: true
    #   script-path: Jenkinsfile
    #   lightweight-checkout: true
